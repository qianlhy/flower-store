server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: flower-store-backend
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    #url: jdbc:mysql://156.227.237.220:3306/flower_store?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    url: jdbc:mysql://156.227.237.220:3306/flower_store?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: flower_store
    password: t8ESJ7k2RERGjDAi
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  # Redis配置
  redis:
    host: 156.227.237.220
    port: 6379
    password: # 如果有密码，在此填写
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

# MyBatis Plus配置
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.flowerstore.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 微信小程序配置（须与微信公众平台该小程序的 AppID、AppSecret 完全一致，否则 jscode2session 会失败）
wechat:
  appid: wx1838a2cff1a8aa71
  secret: 1267c258cf66a9a2c0156fc79456c304
    #088d0c9a3a536cad7501f197a06d60ef

# JWT配置
jwt:
  secret: flowerstore_secret_key_2024
  expiration: 604800000  # 7天，单位毫秒



# 文件上传配置
file:
  upload:
    path: /www/wwwroot/default/uploads/  # 本地开发路径（部署到Linux服务器时改为 /www/wwwroot/default/uploads/）
    static-pattern: /uploads/**  # 静态资源访问路径
    max-size: 10485760  # 最大文件大小，10MB
    domain: https://ru.ruasiagarden.com  # 本地开发域名（部署时改为 https://ru.ruasiagarden.com）

# 订单通知配置
order:
  notification:
    enabled: true
    voice-enabled: true
    voice-text: "您有新的订单，请及时处理"

